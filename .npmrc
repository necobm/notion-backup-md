# Native modules must be compiled against Electron's Node, not system Node.
# These env vars make prebuild-install fetch the correct prebuilt binaries.
runtime=electron
target=35.0.0
disturl=https://electronjs.org/headers
